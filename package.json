{
  "name": "lca-qc-app",
  "version": "1.0.0",
  "description": "LCA Cleaning Services — QC & Training Management",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install",
    "dev": "concurrently \"node backend/server.js\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "seed": "node backend/seed.js",
    "add-properties": "node backend/add-properties.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.1.1",
    "node-sqlite3-wasm": "^0.8.11",
    "nodemailer": "^8.0.4"
  }
}
